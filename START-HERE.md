# 공유 방명록 적용 순서

## v7 적용 — 이 안내를 우선하세요

1. Supabase SQL Editor에서 이번 `supabase-setup.sql` 전체를 다시 실행합니다. 별 상한을 7개에서 8개로 늘리기 때문에 v6 사용자도 필요합니다. 기존 방명록은 삭제하지 않습니다.
2. `portfolio-game-v7` 폴더 내용물을 GitHub 저장소에 반영합니다.
3. 완주 후 별 8개 기록도 방명록에 정상 제출되는지 확인합니다.

배경은 입력 → 대기 → 제자리에서 오른쪽부터 삭제로 변경하고, 줄 수와 밝기를 낮췄습니다. 2024년 순서는 삼성 AI Challenge → 이미지 미학 연구이며, 2025년 첫 항목에 전공 수석 졸업과 첨부 사진을 추가했습니다. 별 8개, 코인 35개입니다.
이번 버전은 반드시 SQL 업데이트가 필요합니다.

## 1. Supabase 설정

1. 만든 프로젝트에서 SQL Editor → New query를 선택합니다.
2. 이 폴더의 `supabase-setup.sql` 전체를 붙여넣고 Run을 누릅니다.
3. 성공 메시지를 확인합니다. 오류가 있으면 배포 전에 오류 내용을 확인하세요.
4. Table Editor에서 `journey_guestbook` 테이블이 생성됐는지 확인합니다.

이 SQL은 방명록 전용 테이블과 두 함수를 생성합니다. 기존 기록을 삭제하지 않습니다.
방문자는 테이블에 직접 접근할 수 없으며 조회·제출 함수만 호출할 수 있습니다.
이 패키지가 실제 Supabase에 실행되거나 배포된 것은 아닙니다.

## 2. GitHub Pages 적용

압축 안 `portfolio-game-v7` 폴더의 내용물을 기존 저장소에 복사합니다.
기존 `.git` 폴더는 그대로 유지하세요. `assets/js/guestbook.js`에는 전달받은 공개 URL/키가 이미 들어 있습니다.
SQL과 이 안내문은 배포할 필요가 없습니다. `index.html`, `game.html`, `assets`는 같은 레벨을 유지하세요.
commit/push 후 기존 포트폴리오 → Game Mode를 엽니다.

## 3. 한 번 검증하기

1. 게임을 끝내고 테스트 닉네임/한마디로 방명록을 제출합니다.
2. 'Published!' 안내를 확인합니다.
3. 관리자 승인 없이 목록에 표시되는지 확인합니다.
4. 다른 브라우저/시크릿 창에서 게임 완주 화면을 열어 공개 기록을 확인합니다.
5. 불필요한 테스트 기록은 Table Editor에서 직접 삭제할 수 있습니다.

## 운영 방식과 한계

- 로그인 없이 제출하며 새 글은 즉시 공개됩니다. 필요하면 Table Editor에서 approved를 false로 바꿔 숨길 수 있습니다.
- 별 0~8, 코인 0~35, 닉네임 1~24자, 메시지 최대 160자를 서버에서 검증합니다.
- 모든 방문자 합산 10초에 1건, 최근 24시간 최대 500건으로 제출량을 제한합니다. 동시에 온 정상 방문자도 잠시 기다릴 수 있습니다.
- CAPTCHA/IP별 제한은 아직 없습니다. 악성 사용자가 제출 한도를 소진하는 것까지 방지하지는 못합니다. 트래픽이 늘면 Edge Function + 서버 측 CAPTCHA 검증을 추가하세요.
- 점수 범위만 검사하며 실제 플레이 여부/완주/점수 진위는 검증하지 않습니다. 재미용 방명록입니다.
- 공개 목록은 숨기지 않은 최신 20건이며, 폼을 열 때 다시 불러옵니다. 실시간 구독 방식은 아닙니다.
- 같은 실행의 전송 재시도는 동일 UUID를 사용해 중복 등록을 막습니다. 새로고침으로 새 실행을 만들 수 있습니다.
- 기존 브라우저 전용 기록은 자동 업로드하지 않습니다. 공개용 동의를 받지 않은 이전 기록을 옮기지 않기 위해서입니다.
- secret/service_role 키나 DB 비밀번호를 HTML/JS에 넣지 마세요.

## v5 변경 사항

이전 SQL을 실행했어도 이번 `supabase-setup.sql` 전체를 다시 실행해야 새 글이 즉시 공개됩니다. 기존 숨김/미승인 기록은 자동 공개하지 않습니다.
게임만 코드 에디터 테마로 변경했습니다. 일반 포트폴리오는 유지됩니다.
교체 이미지: `assets/images/game-motion-aug.png` (2026), `assets/images/game-dnsad.png` (2025).

## 검증 상태

JavaScript 문법과 모의 API 요청 검사를 수행했습니다. SQL은 실제 프로젝트에 아직 실행하지 않았으며 실서버 권한/전송 검증은 위 설정 이후 필요합니다.

참고: https://supabase.com/docs/guides/database/functions
